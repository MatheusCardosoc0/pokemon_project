"use client"

import Modal from '@/components/Modals'
import React from 'react'

export default function LoginModal() {
  return (
    <Modal
      title=''
      actionLabel=''
      onClose={() => {}}
      onSubmit={() => {}}
      body={undefined}
      disabled={false}
      footer={undefined}
      isOpen={false}
    />
  )
}
